export const ADDON_ID = '@storybook-extras/angular';

export const WRAPPERS = `wrappers`;
export const WRAPPERS_PARAM_KEY = `${WRAPPERS}`;
export const WRAPPERS_DECORATOR = `${ADDON_ID}/${WRAPPERS}`;
export const WRAPPERS_TOOL_ID = `${ADDON_ID}/${WRAPPERS}/panel`;

export const DOCS = `docs`;
export const DOCS_PARAM_KEY = `${DOCS}`;
export const DOCS_DECORATOR = `${ADDON_ID}/${DOCS}`;

export const COMPODOC = `compodoc`;
export const COMPODOC_ID = `${ADDON_ID}/${COMPODOC}`;
export const COMPODOC_SOURCE_ID = `__STORYBOOK_COMPODOC_SOURCE__`;


export const SOURCE_CODE = `sourceCode`;
export const SOURCE_CODE_PARAM_KEY = `${SOURCE_CODE}`;
export const SOURCE_CODE_DECORATOR = `${ADDON_ID}/${SOURCE_CODE}`;
export const SOURCE_CODE_PANEL_ID = `${ADDON_ID}/${SOURCE_CODE}/panel`;

export const EVENTS = {
    SET_SOURCE_CODE: `${ADDON_ID}/setSourceCode`,
};
